# v2.8.1

- Snapshot incoming images when they enter the merge queue so delayed processing no longer depends on AstrBot's event-owned temporary files.
- Feed queued image snapshots directly to image caption providers without creating another unmanaged temporary file.
- Add regression coverage for image captioning after the source event has cleaned its media files.