# v2.8.2

- Add an optional end-of-message requirement for skip-delay keywords.
- Keep the new behavior disabled by default for backward compatibility.
- Ignore empty skip keywords and add regression coverage for punctuation inside shared text.
